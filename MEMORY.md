# Project Memory

- NeuralNetUI is a Next.js 16 TypeScript chat application with SQLite-backed authentication, settings, conversations, uploads, and tool-event persistence.
- Docker builds exclude the host `.python` virtual environment because Linux virtualenv interpreter symlinks point outside the build root and cause Turbopack tracing to fail.
- The UI supports English and Korean. Technical tool events use a collapsed message-level group with active tool names or completed/failure totals, nested per-call rows, and display-only localized call markers in reasoning.
- Selectable questions appear one at a time above the composer, support model-chosen single/multiple/ranked modes, and render persisted tool answers as user-style question/answer bubbles.
- Deleting a user message also deletes the immediately connected assistant response from that branch.
- DDGS subprocess JSON I/O is explicitly UTF-8 to avoid Windows code-page corruption and partial JSON responses.
- Page visits support bounded text/JSON/XML, safe raster images, and temporary PDF processing; archives and unsupported binaries are rejected.
- The opt-in Browser tool uses isolated multi-tab Chromium sessions for JavaScript rendering, element-ref interactions, waits, and model-visible screenshots. Desktop builds launch headed Chromium off-screen and expose those exact tabs through an authenticated split view; display-less hosts fall back to headless. Model tab listings include stable IDs, titles, URLs, labels, and notes. Public-address checks cover navigation and subresources. Sessions belong to a conversation and persist across model responses until a user/model explicitly closes the last tab/session or the conversation is deleted; inactivity and capacity limits never evict an existing session.
- Image and PDF uploads share the attachment pipeline. PDF originals and bounded extraction caches live under `data/uploads`; scanned-page renders and URL downloads are temporary and are removed after use.
- Admins configure tool rounds, browser tabs per session, multiple-choice questions per call, attachment/download limits, PDF processing limits, timeouts, and orphan-upload retention in the Harness settings tab.
- Client-side IDs use Web Crypto when available and a collision-resistant fallback on non-secure LAN/Tailscale HTTP origins where browsers hide Web Crypto.
- Frontend design guidance lives in `design/MASTER.md`.
- Per-user default model and reasoning preset are applied on a fresh app session; in-app New Chat preserves the current selection.
- Admins can unload models from the picker even during generation. This is intentional: reducing inference-server load takes priority. Do not add a generation guard to either the UI or unload API.
- User message bubbles use a 17px desktop font and a readable 16px mobile font.
- General settings can export/import validated `neuralnetui-model-settings` v1 JSON with two-space indentation; ownership metadata is never exported and imports save immediately.
- Assistant Markdown supports KaTeX inline and display math. Appearance settings persist per user and control model identifier visibility plus both single- and double-tilde strikethrough rendering; disabling strikethrough preserves the source tildes.
- Mobile/coarse-pointer composer Enter inserts a line break; desktop Enter submits and Shift+Enter inserts a line break.
- Settings tabs scroll horizontally on narrow screens.
- Chat streaming auto-follows only while the thread remains near the bottom, and hidden served models are excluded from alias base-model choices.
- On narrow mobile screens, user-message actions sit below the bubble, which uses nearly the available screen width and a 16px font. Message and code copying falls back to a selection-based path on insecure LAN/Tailscale HTTP origins.
- Unit tests run with `npm test`; type checking uses `npx tsc --noEmit`; the production build uses `npm run build`.
- Windows x64 MSI packaging is driven by `installer/build-msi.ps1` and bundles the standalone Next.js app, Node.js, embedded Python search/PDF dependencies, service host, and tray host.
- Connections are ordered and support OpenAI API or LM Studio drivers. Connections, models, and reasoning presets use drag-and-drop or adjacent up/down controls; their independently scrollable settings lists persist the order shown by chat pickers. Each connection preserves its own discovered model metadata and reasoning presets; duplicate served identifiers resolve to the highest-priority connection.
- LM Studio discovery uses native `/api/v1` REST. Version 2.1.1 uses request-scoped SDK loading and supported chat inference for progress, with Chat Completions fallback for unsupported SDK semantics.
- Chat admission uses a server-wide residency manager: inventory checks and load/unload mutations are serialized; aliases use their base model. Dispatched mutations retain the lock through completion even if the caller cancels.
- Current release version: 3.0.0-rc1-b20 (release candidate; if QA passes only the version changes to 3.0.0).
- RC1 beta 20 has no status polling: the picker checks `GET /api/models/status` on load (only once `config.account` exists), on picker open and on the first key after 60 s without keyboard input (`useKeyboardReturn`); settings dots recheck on draft edits, after detection and on keyboard return. `probeConnection` returns online/error/offline: expected JSON listing = online, any answer otherwise (4xx, 5xx, non-JSON, wrong shape) = error (orange dot, picker models stay selectable with an orange dot and hint), transport/timeout/invalid URL = offline. `lib/connection-errors.ts` classifies detection failures; `/api/models/detect` returns `{error, code, status}` and settings show a danger "모델 감지 실패" dialog. Storage batch mode has select all (up to 100 matching files across pages); multi-file deletes skip in-use files and report them.
- RC1 beta 20 release artifact: `installer/output/NeuralNetUI-3.0.0-rc1-b20-x64.msi`, 305,803,301 bytes, SHA-256 `BCB3B17D0D19AE519C934C6D87CBDA87F357C3135EB888C36B02D1080B73CB7F`, BUILD_ID `k6AaN912Q7FyE9zxu-cUc`. See `docs/audits/2026-09-19-release-3.0.0-rc1-b20.md`. Validation: `scripts/test-beta20-integration.mjs` (`BETA20_APP_DIR`/`BETA20_NODE` absolute paths for the staged package).
- Beta 19 connections have optional `disabled` (schema-default absent = enabled). `lib/chat-runtime.ts` refuses chats on a disabled connection. `lib/connection-status.ts` `connectionStates` returns online/offline/disabled (disabled is never probed); `GET /api/models/status` returns `statuses` plus `offlineConnectionIds`; admin-only `POST /api/models/status` probes the unsaved connection draft (saved keys fill blanks, invalid URL = offline). The picker (`lib/model-availability.ts`) drops disabled-server models, greys out offline ones (`.model-option.offline`, `aria-disabled`, CSS `::after` hover hint), moves an offline selection to the online default or first online model, and shows only "서버 오프라인" when nothing is online; status is fetched on load, on picker open and every 30 s. Connection settings show `.server-status-dot` right of the server icon (15 s poll), an enable switch left of delete, and a danger confirm before delete; the delete button's class is `connection-remove`, and `.connection-card-head .connection-remove` rules replace the old `.connection-card-head button` selectors so the switch keeps `.toggle` styling.
- Beta 19 release artifact: `installer/output/NeuralNetUI-3.0.0-beta.19-x64.msi`, 305,811,400 bytes, SHA-256 `4EADD8A0F1F5F83D0578B78C1E4325C5514D507C15387103E1E03D70A4338F29`, BUILD_ID `IcRgxKlaFop2JjxN165eH`. See `docs/audits/2026-09-18-release-3.0.0-beta.19.md`. Validation: `scripts/test-beta19-integration.mjs` (`BETA19_APP_DIR`/`BETA19_NODE` for the staged package).
- Beta 18 delete confirmations (chat, message+response, selected chats) use tone `danger`. Reset credits empty covered windows, which restart at the first use after the reset (user-confirmed intent; longer windows keep usage and timing). Personal/user backup images add `modelSettings` ({settings: model-settings JSON, ownedModelIds, ownedPresets}); `lib/model-settings-backup.ts` restores it best-effort after the data (own aliases for everyone, served-model settings/private templates/order for admin targets, `alias-<hash>` ids on collisions, lenient per-model parsing) and reports `modelSettings.status` restored/absent/invalid/failed. Standard-account alias templates are now stamped with the owner in `writeConfigForUser`; previously `claimLegacyCustomizations` handed them to the superadmin.
- Beta 18 release artifact: `installer/output/NeuralNetUI-3.0.0-beta.18-x64.msi`, 305,799,016 bytes, SHA-256 `9C6B3D84638A450429253D92FBBD36B13A4143BBEAD1F8358ABF9B539763A4B8`, BUILD_ID `UjwMJve9cxO8G6eYUkdpV`. See `docs/audits/2026-09-17-release-3.0.0-beta.18.md`. Validation: `scripts/test-beta18-integration.mjs` (`BETA18_APP_DIR`/`BETA18_NODE` for the staged package).
- Beta 17 usage meters are white, yellow from 80% and red from 90% (`usageLevel` in `lib/usage-popover.ts`). The client dispatches `USAGE_REFRESH_EVENT` when a chat job reaches a terminal state; the popover has a manual refresh button. `GET /api/usage` includes in-flight streamed tokens from a `globalThis` live registry in `lib/plans.ts` (`setLiveTokenUsage`/`clearLiveTokenUsage`); admission and output-limit checks use recorded events only. `streamTurn` charges server usage or, when absent, local estimates, including partial output from stopped/failed streams.
- Beta 17 `GET /api/models/status` probes each used connection's model-list endpoint (3 s timeout, 5 s cache, transport failure or 5xx = offline); the picker hid models whose `connectionId` was offline (beta 19 greys them out instead). Composer tool switches persist in account preferences `enabledTools` via `PUT /api/preferences/tools`; `updateUserPreferences` keeps `enabledTools` when the settings form saves. Plan weights allow two decimals (server rejects more). A capture-phase document wheel listener in `app/page.tsx` blurs a focused `type=number` field under the pointer, because Chrome otherwise steps its value while the wheel scrolls (this caused saved weights like 1.35→1.11). Workspace-wide `AppConfig.showModelWeights` (admin-editable in Appearance, default false; not a per-account appearance preference) shows grey, vertically centred `x<weight>` badges to every account including superadmins; while on, `publicConfig` sends `modelWeights` from each account's own plan (aliases resolved to the charged served model), and while off it sends none.
- Beta 16 backups use a streamed AES-256-GCM envelope (`NNUIENC1`, `.nnbak`) around 7z, with per-image salt/nonce and scrypt N=32768/r=8/p=1. Credentials are the data owner for personal/per-user images and exporting administrator for global/accounts. Restore verifies the image using its creation-time credentials without consulting source-instance secrets; plaintext beta 15 archives require re-export. Personal/user scopes interoperate, using deterministic destination IDs and rewritten attachment URLs on cross-owner imports. Account restore is superadmin-only, maps usernames to local IDs, and preserves the destination operator. Destination quotas remain enforced.
- Backup APIs bound streamed uploads and serialize backup/restore operations. Personal restore validates graph membership and rolls back newly created files as well as replaced files; post-commit file cleanup cannot trigger a false rollback. The bilingual credentials modal uses existing dialog/pill design tokens.
- Beta 17 release artifact: `installer/output/NeuralNetUI-3.0.0-beta.17-x64.msi`, 305,799,016 bytes, SHA-256 `880B079E91FD15903C9A4CA46E33B998D5155D5524234126A33AD640081D63B9`, BUILD_ID `LzwC4sfE3AqsUdiQ8j3eO`. See `docs/audits/2026-09-17-release-3.0.0-beta.17.md`. `scripts/test-beta15-integration.mjs` fails at its backup step since beta 16 requires backup credentials.
- Beta 16 release artifact: `installer/output/NeuralNetUI-3.0.0-beta.16-x64.msi`, 305,776,587 bytes, SHA-256 `07254A29E51601911E82C4784062EBA46D3A6840CC51078346C0E2C05E70FCC4`, BUILD_ID `UaKAyiT_4hg96fKOiPiQg`. See `docs/audits/2026-09-17-release-3.0.0-beta.16.md`.
- Release 3.0.0-beta.15 adds versioned, high-compression 7z images for personal data and administrator global/auth/per-user scopes. Restore supports merge or replace, rejects future image versions, runs older images through sequential adapters, validates archive paths/sizes/files, and applies database/file changes with rollback-aware staging.
- Beta 15 adds administrator-defined plans with served-model toggles, multiple first-use-anchored input/output/both token windows, per-model weights, storage quotas, per-user assignment, and user/plan reset credits. All model and harness usage is charged; per-account admission is serialized. The sidebar donut shows only the nearest percentage and provides reset-credit redemption.
- Version 3.0.0-beta.15 unsigned MSI was rebuilt and validated on 2026-09-17: `installer/output/NeuralNetUI-3.0.0-beta.15-x64.msi` (305,768,395 bytes), SHA-256 `767E190EE2288A4EF6A16764291AA1C75553D07D84973DAAB408A0013F748866`, BUILD_ID `5bxFkbBo641DcyyBCanje`. It supersedes the 2026-09-16 build. Details, including the backup repair, are in `docs/audits/2026-09-16-release-3.0.0-beta.15.md`.
- Docker's dependencies stage installs Python and C++ build tools for `better-sqlite3` on Node 22. The builder pre-initializes a disposable `/tmp/neural-chat-build-data` database before parallel Next workers run, avoiding first-run migration races. `.dockerignore` excludes MSI/WiX build artifacts, which must never enter the image context.
- Beta 15 follow-up: plans have `trash_quota_bytes` (migration 18) applied with storage on plan save/assign/delete. `/api/users` accepts `sort`, `dir`, `planId`, and `all=1` (identities of every match). `/api/reset-credits` accepts `targetIds` for users and returns `credits`. Plan assignment, reset-credit recipients, and per-user images use the shared directory dialogs in `app/user-directory.tsx`. `lib/plans.ts` imports `./auth` without an extension, so it cannot load under the node test runner; plan behavior is covered in `scripts/test-beta15-integration.mjs`.
- The production MSI install runs service `NeuralChat` as LocalSystem from `C:\Program Files\Neural Chat` with data in `C:\ProgramData\Neural Chat\data`; on 2026-09-17 it was beta 14 (schema 15). `scripts/test-beta15-backup-integration.mjs` accepts `BACKUP_QA_SOURCE` (a data dir, copied through the SQLite backup API) and `BACKUP_QA_APP_DIR`/`BACKUP_QA_NODE` for the staged package. The npm `7zip-bin` Linux binary ships without the executable bit; the Dockerfile and `bundled7z()` both repair it. Any `process.cwd()`-derived path passed to fs/spawn in server code needs `/* turbopackIgnore: true */`, or the standalone output traces the whole repository.
- Beta 15 Docker QA found that Next standalone tracing omitted `playwright-core/browsers.json`, causing repeated background initialization errors after authentication even though the administrator row and session were committed. `next.config.ts` now traces the whole Playwright package. The sidebar usage donut has no adjacent percentage label; its fixed popover is viewport-clamped with an internal height limit. An isolated Docker/browser QA verified setup, login, refresh persistence, a 529x361 popover fully inside the viewport, and no browser/server errors.
- Release 3.0.0-beta.13 edits every long text in the shared large modal (both message edits and both settings prompts), keeps the two settings prompts visible as read-only fields with an edit action, and replaces all nine native confirm dialogs with a four-tone in-app message box. Request actions are hover-revealed over the whole conversation row; storage access moved into the attachment section under a renamed upload action; tool groups lost their rules and gained fold spacing. The rename dialog duplicates a chat with every branch under new identifiers, the history download exports all chats as one ZIP when none is open, and deleting a request that exists in several branches asks for this branch or every branch.
- Version 3.0.0-beta.13 unsigned MSI was built and validated on 2026-09-16: `installer/output/NeuralNetUI-3.0.0-beta.13-x64.msi` (304,874,512 bytes), SHA-256 `47F111881A3A4DC1D7CC458E9446F88F7FCEB7ED158E6E7D8746B28307F36F98`. Source/staged BUILD_ID `oxgViWp039X2zq9--z1ex` and all 73 static assets match; WiX validation plus source and bundled-Node packaged Beta 13 integrations passed. Details are in `docs/audits/2026-09-16-release-3.0.0-beta.13.md`.
- `scripts/test-audit-integration.mjs` has been failing at its preset-preservation assertion (line 80) since before beta 13; the failure reproduces on the unmodified previous commit and is unrelated to the UI releases.
- Release 3.0.0-beta.12 contains host approval cards within the viewport, preserves and wraps explanations with a six-line vertically scrollable maximum, and gives the argument box independent horizontal and vertical scrolling. Long unbroken user-message text such as URLs wraps inside its bubble. Chat cancellation now propagates into active browser, web, storage, upload/PDF, host screen, and host shell work; active browser sessions close to interrupt page work, and Windows shell cancellation terminates the process tree.
- Version 3.0.0-beta.12 unsigned MSI was rebuilt and validated on 2026-09-16: `installer/output/NeuralNetUI-3.0.0-beta.12-x64.msi` (304,854,014 bytes), SHA-256 `D308DBA6FDF939651D0584880BA892C11F5996796AEAE86BBAC5CB94B464DA2C`. Source/staged BUILD_ID `w3cSbZzZM8Z2HRhOGGcVN` and all 73 static assets match; WiX validation plus source and bundled-Node packaged browser integrations passed. Details are in `docs/audits/2026-09-16-release-3.0.0-beta.12.md`.
- Release 3.0.0-beta.10 makes personal storage a viewport-adaptive two-column browser with 3–7 rows and an explicit circular-check bulk-selection mode shared by the attachment picker. Referenced-file deletion opens active-chat references with navigation, selected chat deletion, and an atomic delete-all-related-chats-and-file action. Active chat references, rather than references in already deleted chats, protect files.
- Beta 10 adds a bottom-of-history Manage chats modal with title/content/ID search, inactive-hours/days filtering, cross-page selection, selected deletion, and filter-wide deletion. The sidebar Search/Storage spacing now matches New Chat/Search. Per-model Vision UI is renamed Image input and flattened to one input-plus-switch row; alias-owned image settings continue to override the serving model and are regression-tested.
- Version 3.0.0-beta.10 unsigned MSI was built and validated on 2026-09-15: `installer/output/NeuralNetUI-3.0.0-beta.10-x64.msi` (304,624,554 bytes), SHA-256 `1770521E0EDC80003FF2E0FB09FA9D495D0C15102075B67CD3E2BF79E9F86AB6`. Source/staged BUILD_ID and all 73 static assets match; WiX validation and the bundled-Node packaged integration passed. Details are in `docs/audits/2026-09-15-release-3.0.0-beta.10.md`.
- Release 3.0.0-beta.9 prefers installed stable Chrome/Edge for shared browsing, disables off-screen/background throttling, caches bounded public DNS decisions, reduces browser-frame latency, and suppresses common automation signals for Google sign-in compatibility. Accepted host approvals immediately transition from the approval card to active tool status. Original images remain untouched in storage. Every model card has a Vision group below Description; original input is the default, while bounded mode creates a JPEG cache keyed by the configured 128–8192px long-edge limit. Gemma 4 31B terminates on the 1,600px sample derivative but completes at 1,024px. Personal storage pages by five and supports direct soft delete plus long-press, check-marked, atomic bulk soft delete; saved-chat references remain protected.
- Version 3.0.0-beta.9 unsigned MSI was rebuilt and metadata verified on 2026-09-15: `installer/output/NeuralNetUI-3.0.0-beta.9-x64.msi` (304,598,801 bytes), SHA-256 `5B8AA8D68E7E6B9CE4A56B42CA1047F157C88D56726E7F2396330B48AC21E6BA`. The MSI stores `REBOOT=ReallySuppress` and `REBOOTPROMPT=Suppress`; WiX validation and packaged-standalone storage/Gemma-image integrations passed. Details are in `docs/audits/2026-09-15-release-3.0.0-beta.9.md`.
- Release 3.0.0-beta.7 makes the personal storage capacity meter visibly grey-and-white, removes delete/trash controls from the ordinary storage manager, and adds direct multi-file upload plus Markdown/text file creation. Browser uploads use authenticated 8 MiB chunks with bounded retries so no request approaches Cloudflare Tunnel's 100 MB request limit; server completion verifies every part and the final size before atomically registering the retained owner-only file. The creation modal uses the shared SelectMenu and field language, with GFM/KaTeX Markdown preview.
- Version 3.0.0-beta.7 unsigned MSI was built and metadata verified on 2026-09-14: `installer/output/NeuralNetUI-3.0.0-beta.7-x64.msi` (304,606,933 bytes), SHA-256 `62D173D4F00F746A5AECCC50334E8B77393FBB603CC43D6E2A09FA4D4F0E83DB`. WiX validation and the bundled-Node packaged-standalone smoke test passed; details are in `docs/audits/2026-09-14-release-3.0.0-beta.7.md`.
- Release 3.0.0-beta.6 places Harness storage management immediately after Tool and file limits, separated by deliberate padding and a boundary. The tool-limit validation copy names its scope and sits in a padded panel; storage help starts each sentence on a new line while allowing normal responsive wrapping. Storage controls use matching bordered groups and equal-height numeric/unit controls. Quotas saved as zero inherit the effective workspace default and continue following later default changes; new accounts inherit both defaults. User management is a searchable, 10-per-page modal with split active/trash usage rows and a full-width aggregate card. Tool rounds validate up to 10,000; tool calls remain sequential, so there is no parallel-call limit.
- Beta 6 adds a searchable/sortable storage attachment picker, retains host-selected image/PDF uploads in private storage, and adds an owner-scoped `storage_access` tool that searches and feeds stored images, PDFs, or safe text back into model context. Host `store_file` feeds imported images/PDFs into the same turn. Composer tools are grouped into collapsible Internet, Ambient awareness, Agent, and Interaction sections inside a height-bounded scroller.
- Version 3.0.0-beta.6 unsigned MSI was rebuilt and metadata verified on 2026-09-13 after fixing malformed incremental Next.js static staging. Packaging copies `.next/static` contents into a clean tree, rejects `.next/static/static`, and compares every staged relative asset with the production build. Packaged-standalone browser QA and CSS/JS HTTP checks are recorded with the current SHA-256 in `docs/audits/2026-09-13-release-3.0.0-beta.6.md`.
- Release 3.0.0-beta.5 adds per-admin audit grants controlled only by Superadmin; audit routes require this grant. User-visible conversation/file deletion is now soft deletion with configurable 1–60 day retention, auditor restore/purge, per-user trash quotas defaulting to twice active quota, oldest-first capacity cleanup, and active/trash aggregate meters. Ordinary users cannot enumerate or fetch trash.
- Beta 5 adds live filename search and pagination to personal/admin storage, audit chat search across IDs/titles/all branches with a hard 10-per-page maximum, and near-fullscreen image lightboxes for Markdown and attachments. User-management quota alignment and card spacing are corrected.
- Release 3.0.0-beta.4 adds host `store_file`: requests to attach/show/upload host files default to quota-backed owner-only storage, while `upload_temp` is reserved for explicit temp.hysong.dev sharing. General files use migration 12's unrestricted MIME metadata, but only signature-sniffed raster images/PDFs render inline; all other types download. Authenticated file responses are no-store to prevent cross-account browser-cache reuse.
- Beta 4 splits account/quota settings from paginated administrator conversation and file browsers. Conversations page by 20 and open a full-branch in-app review; files page by 24 with thumbnail/list layouts and SQL-backed name, size, or creation-date sorting. Admin review rewrites owned attachment URLs through the audited target-user endpoint.
- Release 3.0.0-beta.3 replaces the five global partial-trust risk switches with a dedicated 27-row permission-matrix modal. File operations are independent, overwrite/recursive-delete variants are separate, upload/process/screen actions have their own rows, and PowerShell/Bash each expose risk levels 1..5. Every row selects auto-allow or always-ask; unknown actions remain approval-gated. Beta-2 risk choices migrate to equivalent per-operation choices on first read.
- Beta 3 adds per-user owned storage for chat attachments and browser/host screenshots. UI images use authenticated upload URLs; native LM Studio receives local image paths and the OpenAI-compatible fallback materializes base64 only at dispatch. Retained tool images survive orphan cleanup, while saved-chat references block user deletion.
- Storage quotas default to 512MB for new accounts and are configurable from 1MB to 10TB. Atomic SQLite transactions serialize quota reservations and quota reductions. User administration shows per-user and aggregate used/allocated totals, moves role/quota controls into a dedicated modal, and exposes audited full-branch chat ZIP exports plus owned-media browsing/downloads.
- Release 3.0.0-beta.2 fixes browser-driven context overflow. The next-request projection anchors the last authoritative usage and adds later tool/message estimates; browser snapshots have a 32,000-character serialized ceiling; full-page screenshots are capped at 1440x1600 and carry conservative vision-token estimates. Tool-round compaction retains the newest tool request/observation, strips image payloads from text-only summaries, and retries a recognized provider context-overflow response only after bounded compaction. If the corrected request remains above the 95% safety ceiling, the user receives a localized stable error with reported token counts and a viewport/section browsing recommendation.
- Release 3.0.0-beta.1 adds one experimental `host_computer` tool on non-containerized servers. It is disclosed and executable only for Superadmin, with server-side role/feature/environment checks. Actions cover bounded file operations, temp.hysong.dev upload, conversation-scoped background process PID/name tracking, PowerShell/Bash, and model-visible screen capture. Windows service installations capture the interactive desktop through a named-pipe bridge hosted by the signed-in user's tray process instead of capturing service session 0.
- Host permissions live in harness settings: full trust, partial trust with a dedicated per-operation matrix, or no trust (default). The matrix separates file actions and destructive variants, upload/process/screen actions, and each PowerShell/Bash risk level. Every untrusted action waits in an approval card with risk, concrete localized explanation, full arguments, and allow/deny/deny-with-redirection. PowerShell/Bash assessment is a separate model completion with command-only data and no chat history; failed assessments become level 5.
- Settings have three reaches. Per-account preferences live on the user row; workspace settings live in app_config and only administrators write them; `loginAppearance` is workspace-wide and reaches the sign-in screen through the unauthenticated `/api/auth/status`, because the sign-in screen has no account to read an accent from.
- `PUT /api/config/defaults` (admin only) sets one workspace default and overwrites that one key on every user row via `overwriteUserPreference`; the other key and the rest of each person's preferences survive. Surfaced as "전체 적용" beside the personal "기본으로 사용" in the model and reasoning pickers.
- Served models belong to administrators: `writeConfigForUser` carries `current.connections` over unchanged for standard accounts, so they can pin a served model or reasoning level as their own default but cannot edit its prompt or reasoning templates. Custom (alias) models they own stay fully editable. The reasoning panel shows the locked controls disabled rather than hiding them.
- Version 2.1.7 unsigned MSI built and metadata verified on 2026-09-10: `installer/output/NeuralNetUI-2.1.7-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-10-release-2.1.7.md`.
- Release 2.1.8 gives `visit_page` a browser-compatible User-Agent while retaining the NeuralChat product token; OpenAI's incident article changes from Cloudflare 403 to 200. Live reasoning and the full transcript independently pause auto-follow when scrolled away from the bottom and show a down-arrow control that resumes it.
- Version 2.1.8 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-2.1.8-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-2.1.8.md`.
- Release 2.2.0-beta.1 adds an authenticated live browser split view beside chat, backed by the same headed Chromium session used by the model. Users can navigate, click, drag, scroll, type into the focused page, and complete an explicit model-to-human handoff without losing session state. Desktop Chromium is bundled; display-less hosts retain a headless fallback.
- Version 2.2.0-beta.1 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-2.2.0-beta.1-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-2.2.0-beta.1.md`.
- Release 2.2.0-beta.2 adds shared-browser tabs with model-visible titles, URLs, labels, and notes; model/UI tab lifecycle controls; administrator caps for browser tabs and multiple-choice questions; tighter technical-fold rhythm; and user-edge alignment for submitted choice answers.
- Version 2.2.0-beta.2 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-2.2.0-beta.2-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-2.2.0-beta.2.md`.
- Release 2.2.0-beta.3 restores native LM Studio prompt progress after tool rounds, streams context-compaction reasoning/summary into the compaction fold, counts tool context and immediately applies active compaction to the donut, and keeps shared-browser sessions alive across completed responses until explicitly closed.
- Version 2.2.0-beta.3 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-2.2.0-beta.3-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-2.2.0-beta.3.md`.
- Version 3.0.0-beta.1 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-3.0.0-beta.1-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-3.0.0-beta.1.md`.
- Version 3.0.0-beta.2 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-3.0.0-beta.2-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-3.0.0-beta.2.md`.
- Version 3.0.0-beta.3 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-3.0.0-beta.3-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-3.0.0-beta.3.md`.
- Version 3.0.0-beta.4 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-3.0.0-beta.4-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-3.0.0-beta.4.md`.
- Version 3.0.0-beta.5 unsigned MSI built and metadata verified on 2026-09-13: `installer/output/NeuralNetUI-3.0.0-beta.5-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-13-release-3.0.0-beta.5.md`.
- Version 2.1.5 MSI built and metadata verified on 2026-09-09; unsigned. Output: `installer/output/NeuralNetUI-2.1.5-x64.msi`. Validation notes: `docs/audits/2026-09-09-release-2.1.5.md`.
- Windows Installer compares only the first three version fields, so revision-only releases need `MajorUpgrade AllowSameVersionUpgrades="yes"` to replace the installed product. `package.json` carries the four-field version, which is not valid semver; the package is private and never published.
- The context donut starts at the most recent compacted turn and counts the summary in place of the history it covers, so compaction is visible there. The composer leaves its pill shape whenever anything takes a row of its own — queued messages, attachments or an upload in progress.
- Version 2.1.4 MSI built and metadata verified on 2026-09-09; unsigned. Output: `installer/output/NeuralNetUI-2.1.4-x64.msi`. Validation notes: `docs/audits/2026-09-09-release-2.1.4.md`.
- An assistant message carries `steps` (migration 10): ordered reasoning/content/tools/compaction stages. `content` and `reasoning` remain the concatenation, so copy, export and upstream history are unaffected; `lib/transcript.ts` reconstructs the old fixed layout for messages without steps.
- Compaction decides on `projectedInputTokens(estimate, measured)` where measured comes from `contextUsage` — the same figure the composer's donut shows. Discard the measurement right after compaction replaces the message set, or the compacted request immediately re-triggers compaction. Checked both before sending and mid-stream.
- A compaction stage opens when the wait phase turns `compacting-context` and is filled in on completion, so it shows live in place; the wait line stays quiet for that phase to avoid reporting it twice.
- The UI/LM Studio audit in `docs/audits/2026-09-08-ui-lmstudio.md` has 14 addressed findings; #7 is intentional behavior per the user. Settings preserve protected presets and unsaved model edits; aliases resolve their base connection; API-key removal explicitly disables fallback credentials.
- Deleted conversations/users discard their chat jobs. Background saves validate task ownership and conversation existence inside the SQLite transaction. Terminal jobs release full histories and expire after 60 seconds, with at most 64 terminal jobs retained.
- Persisted tool call/result pairs are restored for follow-up requests. Interrupted questions recover on conversation read; active questions can be stopped. SSE errors and incomplete streams are surfaced, and failed client saves restore the unsent draft.
- LM Studio loaded-instance context limits are reflected in the UI. Queued admissions are cancellable; active jobs retain their model through tool rounds and user-input waits.
- Reasoning presets use authoritative server options: off/none becomes Fast, on becomes Thinking only without effort levels, and unsupported models use Default. Chat Completions maps off/on to none/medium; unsupported preset values are omitted.
- Alias reasoning capabilities follow the actual base connection, including base changes and rediscovery. Custom prompt templates, ownership, and ordering survive capability updates. Existing installations should detect models again and save to refresh legacy metadata.
- OpenAI-compatible discovery optionally enriches records from the same server's LM Studio native models endpoint, preserving served identifiers; unavailable native endpoints fall back to standard discovery.
- Connections expose maxResidentModels (0 = unlimited, positive enables managed loading even with On demand off) and modelWaitPolicy (capacity or serial). Same normalized server roots share admission state and the strictest configured settings.
- SQLite migration 7 persists per-server/per-model LFU counts and last-use timestamps. Eviction chooses idle LLM model groups by fewest uses, then oldest use; multiple loaded instances count separately toward the cap.
- Chat snapshots expose transient waitPhase values for session waits, freeing space, model loading, server waits, and response preparation. LM Studio prompt-processing percentages remain authoritative through short stream gaps; only 30 seconds without a server response changes the line to server waiting. Only connection refusal/DNS failures are retried, for up to 30 seconds; ambiguous inference failures are not replayed.
- OpenAI-compatible managed connections probe LM Studio native management before falling back to the existing /api/inference API. Accurate external-app busy detection depends on upstream metadata; guaranteed protection is for this app's jobs. Manual admin unload remains unrestricted.
- Version 2.1.0 MSI built and metadata verified on 2026-09-09, then rebuilt the same day after the output-token and settings-footer fixes; unsigned. Output: `installer/output/NeuralNetUI-2.1.0-x64.msi`. Validation notes: `docs/audits/2026-09-09-release-2.1.0.md`; the 2.0.0 notes are in `docs/audits/2026-09-09-design-2.0.0.md`.

- Harness settings retain tool limits and add rolling/compacting context handling plus optional first-turn title generation before/after the response. Defaults: rolling, 80% compaction threshold, title disabled, task reasoning off; empty model choice resolves the current chat base model.
- Migration 8 adds separately stored branch summaries, protected title metadata, and message context token counts. Summary SHA-256 fingerprints validate the covered upstream history before reuse; original messages remain intact. Harness requests acquire/release residency independently to avoid serial-policy deadlocks.
- Version 2.0 is a full design-language change: pill buttons/inputs/dropdown triggers, larger radius tokens, hue-free surfaces and type, white icons with no plates, and one custom `SelectMenu` replacing every native select. Accent colour is a per-user palette applied through `--accent-rgb`/`--accent-bright-rgb`; never hardcode an accent hue in CSS.
- Appearance settings add the accent palette (six named plus custom hex, previewed live while the settings panel is open) and two streaming presentation options: fade reveal and chunked pacing. Neither changes model input or stored conversations.
- The composer is one pill row that stretches into a rounded rectangle when the draft outgrows the space beside its controls, measured against a hidden mirror span.
- Popovers animate open and closed via `usePopoverPresence`; a styled button must set its own background or the browser's disabled plate shows through.
- The composer stacks when the space left beside its controls is under 150px, not only when the draft overflows, so phones use the stacked layout from the start. `.chat-idle .composer-wrap` must not set horizontal padding or it overrides the narrow-screen value.
- Driver capabilities live in `lib/driver-capabilities.ts` and surface as an info marker per option in the driver picker; the chat wait line never reports a missing capability. `SelectMenu` options accept an `info` table, portalled out of the popover and paired with a visually hidden summary.
- `harnessSettings.maxOutputTokens` caps a response; 0 means the whole remaining context window. Before 2.1.0 `lib/chat-runtime.ts` hardcoded a 4096-token ceiling, which truncated long answers.
- The settings footer tracks a JSON snapshot of the saved config: save is disabled while clean and the secondary button reads Close instead of Cancel.
- Temporary chats use migration 9's `conversations.temporary` flag: excluded from history and search, swept by `discardTemporaryConversations` when the client's history fetch no longer names them via `keepTemporary`, and skipped while a chat job is registered. Only `PATCH { promote: true }` clears the flag; ordinary saves carry it forward.
- Greetings live in `lib/greetings.ts`: seven time bands and five editable lines per language in appearance.greetings. Main-screen entries choose randomly, excluding the previous visible line stored per account/language/band in sessionStorage; typing does not reroll. Empty lines fall back to defaults; {name} inserts the first name.
- Reasoning descriptions come from `lib/appearance.ts`; an empty override falls back to the built-in wording for the active language, so switching language keeps untouched notes readable.
- The tray host opens the Web UI only when launched without `--startup`; the Run key passes `--startup` so signing in never opens a browser, while the Start menu shortcut passes `--open`.
- The browser tool is gated by `experimental.browserTool` in app config: an admin switches it on in the Experimental settings tab, and `lib/chat-runtime.ts` refuses it server-side regardless of what a client sends.
- Harness settings use the shared section heading, radio cards for context mode and title timing, a slider for the compaction threshold, one bordered block per mode-dependent group, and tool limits split into tool loop, attachments, PDF and cleanup groups. The sidebar has a compact wordmark header with one icon control, and history rows keep rename and delete in a single hover-revealed action group.
- History search reads original content from all user-owned branches, returning up to 100 branch matches with snippets and a non-active-branch indicator. PATCH conversation renames protect manual/generated titles from stale full saves. Desktop sidebar collapse is localStorage-backed.

- Release 2.1.1 adds context history/draft estimates with input/response/reasoning breakdown and immediate sendReasoning toggle updates. Context compaction/rolling can reduce actual upstream input; the UI says estimate.
- Appearance lmStudioProgress selects text/percent/donut/both. Request-scoped @lmstudio/sdk 1.5.0 supplies actual model-load and prompt progress; integer progress changes bypass text batching so cached prompts are visible.
- LM Studio driver uses native SDK inference for supported requests. Named reasoning efforts and separate prior reasoning are kept on Chat Completions because SDK 1.5.0 cannot represent them; prompt progress is explicitly unavailable. Native tool IDs/history, data-URL images, cancellation, and residency locks remain supported. Native inference failures are never replayed.
- Admin-only experimental.openAIProgress defaults false; when enabled, OpenAI connections probe their own native LM Studio inventory before native inference. Other hosts remain standard Chat Completions with progress unavailable. Standard token usage is never interpreted as prompt completion percentage.
- Validation script scripts/test-progress-integration.mjs uses an isolated DB; --live opts into localhost:1234 and --keep leaves the QA server running for browser checks.
- Final 2.1.1 unsigned MSI built and metadata verified: installer/output/NeuralNetUI-2.1.1-x64.msi (400009049 bytes); audit docs/audits/2026-09-09-release-2.1.1.md includes hash and validation. Staged standalone native inference smoke passed.
- Version 2.1.2 MSI built and metadata verified on 2026-09-09; unsigned. Output: `installer/output/NeuralNetUI-2.1.2-x64.msi`. Validation notes: `docs/audits/2026-09-09-release-2.1.2.md`.

- Release 2.1.3 context compaction checks streamed content, reasoning and tool arguments per SSE event. A request-scoped AbortController interrupts inference without cancelling the job; model residency is released for summaries and reacquired for resume. Partial tool calls are summarized as data and never executed.
- Harness resumePrompt supports literal single-pass %COMPRESSED% and %USER_PROMPT%; maxCompactionResumes defaults to 3 (0–100), per response, excluding pre-send compaction. Resume retries do not consume tool rounds. Original prompts/attachments and visible partial output are retained; impossible post-compaction input fails before dispatch.
- Summary requests split oversized serialized history into bounded fragments, including prior summary in each request. Branch summary fingerprints still protect reuse. New settings have schema defaults for existing installations.
- Version 2.1.3 unsigned MSI built and metadata verified: `installer/output/NeuralNetUI-2.1.3-x64.msi` (400025433 bytes); release notes and hash in `docs/audits/2026-09-09-release-2.1.3.md`.

- Release 2.1.5 temporary chats override only background accent channels with neutral grey. Idle temporary copy is fixed; both empty-chat mode controls are icon-only; before the first message the action returns to regular mode preserving the draft, afterwards it promotes the chat.
- New Chat opens `/`; only a successful conversation creation replaces it with `/chat/:id`. Popstate to `/` resets the draft; missing historical draft URLs recover to `/`.
- Composer paste leaves plain text insertion native and routes image-only clipboard files through the attachment uploader. Mixed text/image clipboard data prioritizes text.
- Account settings owns the display-name editor in its own account-card, aligned with the password card, using the shared Save Changes footer. All roles may update their own display name via existing config persistence.

- Release 2.1.6 uses Lightbulb for all reasoning icons; reasoning folds are unindented without a left rule and use right/down chevrons. A per-conversation in-memory eye toggle on the active chat hides technical activity via CSS, retaining mounted disclosure state and user question/answer bubbles. New streaming and lazily loaded records inherit the thread visibility. Reload resets visibility.
- Composer tool order ends with current time, location, optional browser, and multiple choice.
- Version 2.1.6 unsigned MSI built and metadata verified: `installer/output/NeuralNetUI-2.1.6-x64.msi`; validation and SHA-256 in `docs/audits/2026-09-09-release-2.1.6.md`.
