"use client";
import { RotateCcw, X } from "lucide-react";
import { useEffect, useState } from "react";
import type { UsageStatus } from "@/lib/types";
function duration(seconds:number,ko:boolean){const hours=seconds/3600;return hours>=24&&hours%24===0?`${hours/24}${ko?"일":"d"}`:`${hours}${ko?"시간":"h"}`;}
export function UsageDonut({ko,collapsed}:{ko:boolean;collapsed:boolean}){const[data,setData]=useState<UsageStatus>(),[open,setOpen]=useState(false),[busy,setBusy]=useState("");const load=()=>fetch("/api/usage",{cache:"no-store"}).then(response=>response.ok?response.json():undefined).then(setData).catch(()=>undefined);useEffect(()=>{void load();const timer=window.setInterval(load,60_000);return()=>window.clearInterval(timer);},[]);if(!data?.plan)return null;async function redeem(id:string){setBusy(id);try{const response=await fetch("/api/usage",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({creditId:id})});if(response.ok)setData(await response.json());}finally{setBusy("");}}return <div className="usage-donut-wrap"><button className="usage-donut-button" title={ko?"플랜 사용량":"Plan usage"} aria-label={ko?"플랜 사용량":"Plan usage"} onClick={event=>{event.stopPropagation();setOpen(value=>!value);}}><span className="usage-donut" style={{"--usage-fill":`${Math.min(100,data.nearestPercentage)*3.6}deg`} as React.CSSProperties}/>{!collapsed&&<small>{Math.round(data.nearestPercentage)}%</small>}</button>{open&&<div className="usage-popover"><header><div><strong>{data.plan.name}</strong><small>{ko?"가장 한도에 가까운 구간":"Window nearest its limit"}</small></div><button onClick={()=>setOpen(false)}><X size={15}/></button></header>{data.windows.length?<div className="usage-window-list">{data.windows.map(window=><div key={window.id}><span>{duration(window.durationSeconds,ko)} · {window.tokenScope}</span><b>{Math.round(window.percentage)}%</b><i><em style={{width:`${window.percentage}%`}}/></i></div>)}</div>:<p>{ko?"토큰 한도가 없습니다.":"No token limits."}</p>}{data.credits.length>0&&<div className="usage-credit-list"><strong>{ko?"리셋권":"Reset credits"}</strong>{data.credits.map(credit=><button key={credit.id} disabled={busy===credit.id} onClick={()=>void redeem(credit.id)}><RotateCcw size={14}/><span>{credit.title}<small>{duration(credit.maxWindowSeconds,ko)} {ko?"이하 초기화":"and shorter"}</small></span></button>)}</div>}</div>}</div>;}
