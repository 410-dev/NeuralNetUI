# 3.0.0-beta.12 validation

## Scope

- Kept host approval cards and their contents inside the conversation viewport, including narrow layouts.
- Preserved authored line breaks in approval explanations, wrapped long paths and commands, and limited the visible explanation to six lines before vertical scrolling.
- Ended the argument disclosure at the card's inner edge and enabled independent horizontal and vertical scrolling for its structured payload.
- Wrapped long URLs and other unbroken user-message text inside the message bubble without widening the chat or document.
- Propagated chat cancellation into active browser, web, storage, upload/PDF, host screen, and host shell operations. Active browser sessions close to interrupt pending page actions, and Windows shell cancellation terminates the running process tree.
- Bumped the application and package display version to `3.0.0-beta.12`.

## Automated verification

- `npm test`: 146 tests passed, including a host-shell cancellation regression that aborts a 30-second command in under five seconds and prevents its completion.
- `npx tsc --noEmit`: passed.
- `npm run build`: passed TypeScript checking and the Next.js 16.3.3 production build.
- Source Beta 12 Chromium integration: passed at 820×760. It asserted that an extremely long unbroken URL remains inside the user bubble with no document-level horizontal overflow; the approval card stays inside the viewport; the explanation wraps, scrolls, and exposes at most six lines; and the argument payload scrolls in both axes inside the card.
- The same integration allowed a 20-second PowerShell tool call, pressed **생성 중단** while it was active, observed the stopped chat/error tool state, and confirmed that the command's sentinel file was never written.
- Manual in-app browser QA visually confirmed the contained approval card, six-line explanation scroller, and two-axis argument scroller.
- Packaged-standalone Beta 12 Chromium integration passed using `installer/build/stage/node.exe` and the staged app, including the long-link and active-tool cancellation checks.

## Packaging verification

- `installer/build-msi.ps1` completed successfully for the final Beta 12 source tree.
- Source and staged BUILD_ID values are identical: `w3cSbZzZM8Z2HRhOGGcVN`.
- All 73 production static files match the staged relative paths and SHA-256 values. No `.next/static/static` directory exists.
- The staged package reports `3.0.0-beta.12`.
- `wix msi validate`, with the established ICE60 unversioned-file and ICE61 same-numeric-version beta-upgrade warnings suppressed, exited successfully.
- MSI properties: ProductName `NeuralNetUI`, ProductVersion `3.0.0`, `REBOOT=ReallySuppress`, and `REBOOTPROMPT=Suppress`. No reboot actions are scheduled.
- Authenticode status is `NotSigned`.

## Artifact

- Output: `installer/output/NeuralNetUI-3.0.0-beta.12-x64.msi`
- Size: 304,854,014 bytes
- SHA-256: `D308DBA6FDF939651D0584880BA892C11F5996796AEAE86BBAC5CB94B464DA2C`
