# 2.1.8 validation

## Scope

- Fix public page visits rejected solely because `NeuralChat/1.0 (+local page reader)` looks like a bot-only User-Agent.
- Let readers pause automatic scrolling independently in live reasoning and in the full transcript, with an explicit control to return to the latest content.

## Root cause and correction

- A direct request to the reported OpenAI URL returned Cloudflare HTTP 403 with the old User-Agent.
- The same request returned HTTP 200 with a Chromium-compatible prefix, both with and without the trailing NeuralChat product token.
- Page visits now send the accepted compatibility prefix, the application version in a NeuralChat product token, the existing bounded Accept list, and a normal language preference.

## Validation

- `npm test`: 119 tests passed.
- `npx tsc --noEmit`: passed.
- `npm run build`: passed.
- In-app browser QA against an isolated database and a slow mock SSE model: moving the transcript away from the bottom revealed its resume control; activating it returned to the latest output and removed the control. Moving the live reasoning pane to its first lines kept it there while new lines arrived and revealed its own resume control; activating it restored live following. Browser console warnings/errors: none.
- Reported URL smoke test: old User-Agent 403; new User-Agent 200 and HTML response.

## Windows package

- Output: `installer/output/NeuralNetUI-2.1.8-x64.msi`.
- ProductName: NeuralNetUI; ProductVersion: 2.1.8.
- UpgradeCode: `{8D444D86-37C2-4E37-B56B-6BB9CD2C62BD}`; ProductCode: `{20583AB8-3FB8-480E-A826-3E3F1A73DCFD}`.
- Size: 400,159,004 bytes. Authenticode: NotSigned.
- SHA-256: `F0EB76FB2F1538D76B1704D208E9AB1BFDE2928F1DAF426B91539E0BFFC1BA79`.
- Source and staged app BUILD_ID: `G1ciTcJ27IeO2w4eqWDsi`; staged package version: 2.1.8.
- Installation/upgrade execution was not performed.
