# 3.0.0-beta.11 validation

## Scope

- Styled the per-model maximum-long-edge input with the shared neutral pill field language, including disabled and focus states.
- Vertically aligned the maximum-long-edge switch with the input body instead of the combined label-and-input stack.
- Removed the Manage chats launcher icon and session icons from both Manage chats and Chats using this file.
- Fixed Manage chats at ten sessions per page. While selection mode is active, clicking a session body toggles that session instead of navigating.
- Changed chat-session containers to full pills in both dialogs.
- Increased personal-storage card radius to 22px and made the preview's top and bottom spacing equal.
- Fixed an existing response race: aborting the first responsive page-size request can no longer turn a successful-but-interrupted JSON parse into an empty object that crashes the dialog.

## Automated verification

- Red-state browser regression: the Beta 11 test initially failed because the maximum-long-edge input rendered with the browser's white native background.
- Red-state alignment regression: the rebuilt Beta 11 test measured the switch centre 8px above the input centre before the CSS correction.
- `npm test`: 144 tests passed.
- `npm run build`: passed TypeScript checking and the Next.js 16.3.3 production build.
- Source Beta 11 Chromium integration: passed in installed stable Chrome at 1280×850. It asserted the styled long-edge input, input/switch centre alignment within 1px, icon-free launcher/session rows, 10-item chat pages, non-navigating body selection, full-pill row radii, 22px storage-card radius, and equal preview top/bottom spacing.
- Manual in-app browser QA: passed for the model input, sidebar launcher, storage cards, related-chat pills, the 10-of-13 first Manage chats page, and a session-body click changing the visible selected count from zero to one without leaving the modal.
- Packaged-standalone Beta 11 Chromium integration: passed using `installer/build/stage/node.exe` and the staged app.

## Packaging verification

- `installer/build-msi.ps1` completed successfully for the final Beta 11 source tree.
- Source and staged BUILD_ID values are identical: `AWLwrmvivjI94decIYrXw`.
- All 73 production static files match the staged relative paths and SHA-256 values. No `.next/static/static` directory exists.
- The staged package reports `3.0.0-beta.11`.
- `wix msi validate`, with the established ICE60 unversioned-file and ICE61 same-numeric-version beta-upgrade warnings suppressed, exited successfully.
- MSI properties: ProductName `NeuralNetUI`, ProductVersion `3.0.0`, `REBOOT=ReallySuppress`, and `REBOOTPROMPT=Suppress`.

## Artifact

- Output: `installer/output/NeuralNetUI-3.0.0-beta.11-x64.msi`
- Size: 304,624,554 bytes
- SHA-256: `2F8BE9E1D531C07F17C934BC588EDDF0F55D71B4E5D850ACE19C2C555C9564C6`
