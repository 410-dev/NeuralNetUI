# 3.0.0-beta.9 validation

## Browser rendering and Google sign-in compatibility

- Windows shared browsing now prefers an installed stable Google Chrome, then stable Microsoft Edge, before the bundled Chrome for Testing fallback. Explicit administrator configuration remains highest priority.
- The headed off-screen browser disables occlusion, background timer and renderer throttling. Public DNS decisions use a bounded 30-second cache instead of resolving the same host for every subresource, and the live frame poll interval changed from 700ms to 400ms.
- Playwright's `--enable-automation` default is omitted, the AutomationControlled Blink feature is disabled, and `navigator.webdriver` is hidden in new isolated contexts.
- The browser unit regression verifies executable priority and launch flags. The live Google smoke opened `https://accounts.google.com/` in the selected installed stable browser without an immediate unsafe-browser rejection. No account credentials were used, so an authenticated account-completion flow was not performed.
- Source and packaged-standalone browser-view integration passed headed multi-tab state, tab labels, authenticated user controls, model-to-human handoff, response persistence, and explicit close.

## Tool approval and large-image handling

- Accepting any waiting tool input now changes its persisted/live event from `waiting` to `calling` before execution resumes. The host approval composer therefore unmounts immediately and the existing tool activity UI displays active work during a long operation.
- Stored and downloaded images remain byte-for-byte original. Each model card now has a Vision group below Description. `Use original images` is enabled by default; disabling it reveals a 128–8192px long-edge input (1,024px initial value). Bounded mode creates an atomic JPEG derivative cache keyed by resolution, so models with different limits never reuse one another's copy.
- Attached images, private-storage reads, imported host images and host screenshots all receive the active model's Vision settings. Model rediscovery and model-settings JSON export/import preserve those settings.
- Direct SDK reproduction with `gemma4-31b-qat-uncensored-hauhaucs-balanced-mtp@q4_k_m` showed the earlier 1,219x1,600 / 323,946-byte derivative terminating the LM Studio model instance at prompt-processing 0%. The 780x1,024 / 166,880-byte and 683x896 / 134,996-byte variants both completed prompt processing and returned correct visual descriptions.
- The end-to-end source and packaged-standalone tests uploaded `sample image.png` through NeuralNetUI, attached it to a conversation and received a Gemma 4 response. The stored 2,048x2,688 / 6,242,426-byte PNG retained SHA-256 `CDC448081A24FF703E70601B1605DB38EA98D6A22F9E77946D455E4AE2FF8362`; only the 780x1,024 / 166,880-byte JPEG entered model context.
- Browser QA verified the Vision group order (`Description`, `Vision`, `System prompt`), original-mode default, conditional maximum-resolution input, and saved numeric changes.

## Personal storage

- The personal storage manager requests exactly five files per page.
- A trash icon performs single-file soft deletion. A 500ms press (including mobile context-menu interception) enters check-marked multi-select mode, where up to 100 files can be deleted atomically.
- Files referenced by saved chats remain visibly protected and cannot be included in deletion. The server validates ownership, existence and reference protection for the complete batch before updating any row.
- Source and packaged-standalone UI integration created seven files, verified 5/2 pagination, exercised single deletion and long-press two-file deletion, confirmed five records in the audit trash view, and checked the 390x844 layout for horizontal overflow.

## Application validation

- `npm test`: 144/144 tests passed.
- `npx tsc --noEmit`: passed.
- `npm run build`: passed, including production TypeScript and static generation.
- Source Beta 9 UI integration: passed.
- Source and packaged-standalone Gemma 4 image integration: passed with the configured 1,024px limit.
- Source Google Accounts browser smoke: passed (unauthenticated sign-in surface only).
- Source browser-view integration: passed.
- Packaged-standalone Beta 9 UI and browser-view integrations: passed using the staged bundled `node.exe`.

## Package validation

- `installer/build-msi.ps1` completed successfully for the final source tree.
- Source and staged BUILD_ID values are identical: `3lbIUKaWuS7DFkEYhe2kk`.
- All 73 production static files match by relative path and SHA-256; no staged differences were found.
- The staged package reports `3.0.0-beta.9`.
- WiX MSI validation with the existing ICE60 (unversioned bundled files) and ICE61 (intentional same-numeric-version beta replacement) warnings suppressed exited with code 0.
- Decompiled MSI properties contain `REBOOT=ReallySuppress` and `REBOOTPROMPT=Suppress`, and no `ForceReboot` or `ScheduleReboot` action is present.
- Interactive installation/upgrade was not performed. Authenticode status is `NotSigned`.

## Windows package

- Output: `installer/output/NeuralNetUI-3.0.0-beta.9-x64.msi`.
- Size: 304,598,801 bytes.
- SHA-256: `5B8AA8D68E7E6B9CE4A56B42CA1047F157C88D56726E7F2396330B48AC21E6BA`.
- ProductName: NeuralNetUI; ProductVersion: 3.0.0.
- ProductCode: `{3F95A0B5-651B-427E-92C7-4CDA9835AEB5}`.
- UpgradeCode: `{8D444D86-37C2-4E37-B56B-6BB9CD2C62BD}`.
