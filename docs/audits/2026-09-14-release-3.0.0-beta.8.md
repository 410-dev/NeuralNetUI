# 3.0.0-beta.8 validation

## Response editing and chat images

- Editing an assistant response now replaces stale delivered-content transcript steps as well as the aggregate `content` field. Reasoning, tool, and compaction activity remains in its original order, with the corrected answer stored as the final delivered-content step.
- Chat Markdown keeps custom renderer identities stable across parent scroll-state renders, so an image DOM node is not remounted merely because automatic scroll following changes.
- The isolated browser regression seeded a response with two old content steps around a tool call, edited it through the UI, and verified the active branch after a server read contained only the corrected content step.
- The same regression rendered a 2800x2800 authenticated image between long text blocks. Four complete up/down scroll cycles kept the original image DOM node and produced exactly one original-image request on desktop (1440x900) and mobile (390x844) viewports.
- Browser-plugin visual QA confirmed the corrected `2 / 2` response revision, the long chat layout, the large inline image, and the near-viewport image lightbox.

## Application validation

- `npm test`: 140/140 tests passed, including the new transcript-edit regressions.
- `npx tsc --noEmit`: passed.
- `npm run build`: passed during MSI packaging; production compilation, TypeScript, page data, and static generation completed.
- Source-tree Beta 8 browser integration: passed.
- Packaged standalone Beta 8 browser integration, launched with the staged bundled `node.exe`: passed for response editing plus desktop/mobile image stability.
- Packaged standalone browser-view smoke test: passed for headed multi-tab state, authenticated controls, model/user handoff, response persistence, and explicit session cleanup.

## Package validation

- `installer/build-msi.ps1` completed successfully. The standalone Next.js app, Windows service and tray hosts, Node.js, Chromium 1234 / Chrome for Testing 151.0.7922.34, embedded Python 3.13.14, and search/PDF dependencies were staged.
- Source and staged app BUILD_ID values are identical (`KTdYvUNSnbrgNTxyKxBqe`). All 73 production static files match by relative path and SHA-256, with no missing, changed, or nested `static/static` assets. The staged package reports `3.0.0-beta.8`.
- WiX MSI validation exited successfully. Full validation reports the existing ICE60 warnings for bundled unversioned files and ICE61 because same-numeric-version upgrades are intentionally enabled for beta replacement; validation with those known warnings suppressed exited cleanly.
- Interactive installation/upgrade was not performed. Authenticode status is `NotSigned`.

## Windows package

- Output: `installer/output/NeuralNetUI-3.0.0-beta.8-x64.msi`.
- ProductName: NeuralNetUI; ProductVersion: 3.0.0 (MSI numeric version for application version 3.0.0-beta.8).
- UpgradeCode: `{8D444D86-37C2-4E37-B56B-6BB9CD2C62BD}`; ProductCode: `{3CA19745-3E21-44A6-B595-27532FCE3847}`.
- Size: 304,594,645 bytes. Authenticode: NotSigned.
- SHA-256: `D11901CB37BDD8B987DC1084440B08694608B58E75072FA4C916250B4D25D3D8`.
- Source and staged app BUILD_ID: `KTdYvUNSnbrgNTxyKxBqe`; staged package version: 3.0.0-beta.8.
